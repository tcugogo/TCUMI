<UserControl x:Class="Basic_Streaming_NET.Views.Select_Patient"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Basic_Streaming_NET.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <TextBlock x:Name="Lable_Select" 
               Foreground="Black" 
               Text="選擇個案" 
               FontSize="40" 
               FontFamily="Arial" 
               FontWeight="Bold" 
               TextAlignment="Center" 
               VerticalAlignment="Center" 
               HorizontalAlignment="Center" 
               Margin="0" />
        </Grid>
        <Grid>
            <ComboBox x:Name="folderComboBox_Name" 
                      Width="210" 
                      Height="60" 
                      FontSize="20" 
                      Margin="0,0,0,0" 
                      HorizontalContentAlignment="Center" 
                      VerticalContentAlignment="Center" 
                      SelectionChanged="folderComboBox_Name_SelectionChanged" />
        </Grid>
        <!--<Grid>
            <Button Content="testmark" Height="100" Width="100" Margin="500,200,10,0" Click="testmark"/>
        </Grid>-->
        <!--<Grid x:Name="UserDate_Record_Grid">
            <Button x:Name="UserDate_Record"
            Click="btn_UserDate_Record"
            Width="170"
            Height="80"
            VerticalAlignment="Center" 
            HorizontalAlignment="Center" 
            Background="#2c3e50"
            Foreground="White"
            BorderBrush="Transparent"
            FontSize="16"
            FontWeight="DemiBold"
            Opacity="0.8">
                <Button.Content>
                    執行
                </Button.Content>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Grid>
                                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#34495e"/>
                                            <Setter TargetName="border" Property="BorderBrush" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>-->

        <Grid x:Name="Add_Patuent_Grid">
            <Button x:Name="Add_Patuent"
                Click="btn_Add_New_Patient"
                Width="170"
                Height="80"
                VerticalAlignment="Center" 
                HorizontalAlignment="Center" 
                Background="Red"
                Foreground="White"
                BorderBrush="Transparent"
                FontSize="16"
                FontWeight="DemiBold"
                Opacity="0.8">
                <Button.Content>
                    新增個案
                </Button.Content>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Grid>
                                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#FF7575"/>
                                            <Setter TargetName="border" Property="BorderBrush" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <Grid Name="CheckingButton_Grid">
            <Button x:Name="CheckingButton" 
                    Click="CheckingButton_Click" 
                    Content="查看" Width="170" 
                    Height="80" VerticalAlignment="Center" 
                    HorizontalAlignment="Center"  
                    Background="#2c3e50" 
                    Foreground="White" 
                    BorderBrush="Transparent" 
                    FontSize="16" 
                    FontWeight="Bold" 
                    RenderTransformOrigin="-0.752,-0.95" 
                    Opacity="0.8">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Grid>
                                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#34495e"/>
                                            <Setter TargetName="border" Property="BorderBrush" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>

</UserControl>
